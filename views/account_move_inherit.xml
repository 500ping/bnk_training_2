<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="account_move_form_inherit" model="ir.ui.view">
        <field name="name">account.move.form.inherit</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="after">
                <widget name="web_ribbon" bg_color="bg-danger" title="Overdue" attrs="{'invisible': [('has_overdue_interest', '!=', True)]}"/>
            </xpath>

            <xpath expr="//field[@name='id']" position="after">
                <field name="has_overdue_interest" invisible="1"/>
            </xpath>

            <xpath expr="//button[@name='preview_invoice']" position="after">
                <!-- Add your fields or attributes here -->
                <button name="action_test_inherit" type="object" string="Test"></button>
            </xpath>

            <xpath expr="//page[@name='other_info']" position="after">
                <!-- Add your fields or attributes here -->
                <page string="Payments history">
                    <field name="payment_history_ids">

                    </field>
                </page>
            </xpath>
        </field>
    </record>

</odoo>